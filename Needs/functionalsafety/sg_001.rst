Prevent Unauthorized Message Delivery
========================================

.. sg:: Prevent unauthorized message delivery
   :id: SG_001
   :version: 1.0.0
   :status: approved
   :links: ORG_SMS_001
   :standard: ISO 26262-3 clause 6 (HARA)

   The vehicle function shall not act on messages delivered to an
   application instance that was not an authorized subscriber, as this
   could trigger an unintended actuation.

.. note::
   ``:links: ORG_SMS_001`` — organisation/governance/functionalsafety/
   org_fusa_requirements.rst states plainly that its organizational
   safety management system requirements "are prerequisites that must
   exist before any product-level safety work (HARA, safety goals,
   FSR/TSR/SSR) can be considered valid." This is the first real,
   dead-link-checked citation of that prerequisite from a product-level
   need — resolved via ``needs_external_needs`` (see ``conf.py``)
   against organisation/governance/'s exported ``needs.json``, rather
   than an unenforced prose reference. Previously no need anywhere in
   ``needs/`` cited any ``ORG_`` id at all, checked or not.
