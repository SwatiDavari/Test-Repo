Unit Test Report — diagnostics
=================================

``TC_UNIT_Z_001`` — not yet executed. No automated test exists under
``source/`` implementing this case; ``status: draft`` in
``test cases/index.rst`` reflects that. This report will record the
first real pass/fail result once that test is written and run — it does
not carry over the ``EXEC_BUILD_2026_081`` / ``SUITE_RELEASE_SMOKE``
result from the root ``integration test/testreports/product-verification-report.rst``,
which is a system-level result for a different test case entirely.
