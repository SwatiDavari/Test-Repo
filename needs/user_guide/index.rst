(Retired — moved back)
==========================

This page briefly held the product/safety user manuals; they moved back
to ``doc/manuals/`` (see ``doc/README.md``) on 2026-08-21, so this file
is no longer referenced from anywhere. Left in place — and excluded from
the build via ``needs/conf.py``'s ``exclude_patterns`` — because this
tool can't delete files from the machine it's running on, only rename
them.
