# markdown

Input-source markdown that isn't generated Sphinx-needs output — design notes, ADR drafts, working docs that later get formalized into `Needs/` or `doc/`. Nothing here is built or tested; it's a staging area for prose before it becomes a tracked need or a published doc.
